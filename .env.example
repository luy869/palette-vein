# 本番環境用の環境変数。.env にコピーして値を設定してください。
# .env は .gitignore に含まれており、コミットされません。

# JWT署名シークレット（openssl rand -hex 32 などで生成）
JWT_SECRET=change_me_to_a_random_secret

# フロントエンドのオリジン（本番ドメインに変更）
ALLOWED_ORIGIN=https://your-domain.example.com
