# PaletteVein 設計書

## プロジェクト概要

好みを学習し、その理由を可視化しながら壁紙イラストを発掘するWebサービス。

「好みの色彩（Palette）の鉱脈（Vein）を掘り出す」というコンセプトに基づく。

## 動機・解決する課題

### 実体験ベースの課題

PCのデスクトップ壁紙を探す際、以下の不便を経験した。

1. **フィルタリング問題**: 壁紙としては使えない縦長イラストが大量に混ざっており、横長の壁紙を効率的に探せない
2. **発掘問題**: まとめサイトや既存サービスでは有名作品や微妙な作品ばかりで、自分好みの「掘り出しもの」に辿り着けない

### 既存サービスへの不満

Spotifyや各種SNSの「あなたへのおすすめ」はブラックボックスで、**なぜそれがおすすめされているのかユーザーには分からない**。好みの理由が見えないため、自分の嗜好を理解したり広げたりすることができない。

### 解決アプローチ

- 解像度・アスペクト比による自動フィルタリング
- CLIPによる画像特徴量化と類似検索
- **おすすめの理由を可視化**することで、好みの透明性を確保

## 機能要件

### コア機能

1. **画像の閲覧とフィードバック**
   - 取得済み画像をユーザーに提示
   - いいね / スキップによる反応収集

2. **好みの学習**
   - コールドスタート対策として、初期はランダム or 人気順で表示
   - 反応が一定量蓄積後、好みベクトルの算出を開始
   - 手持ち画像のアップロード（任意）により学習を加速

3. **類似検索による発掘**
   - 学習した好みベクトルを基に類似画像を推薦
   - **推薦理由の可視化**（M2でPoC、M3で確定）
     - (a) CLIPテキストエンコーダで定義済みタグ語彙（「夜景」「淡い色」「人物」など）との類似度を算出し、上位タグを提示
     - (b) いいね済み画像のうち推薦元になったものを併記（最低限の透明性）— M2で先行実装
     - (c) いいね画像群のクラスタリングで「あなたが好む系統X」「系統Y」と命名し、どの系統に近いかを表示
   - M2 では (b) のみ実装し、(a)(c) は M3 で評価して採用判断

### UX方針

- 画像アップロードは強制しない（離脱防止）
- 使い込むほど精度が上がるグラデーション設計

### マルチユーザー対応

- per-user 前提で設計する。DBスキーマに最初から `user_id` を含める
- 当面は自分専用（`user_id=1` で動作）、認証実装は M3 以降
- 好みベクトル・反応データ・アップロード画像はすべて `user_id` でスコープ

## 技術選定

### バックエンド: Go

好みデータの蓄積に伴い検索処理が増加するため、実行速度と並列処理性能に優れたGoを採用。実装現場での採用実績も豊富で、エコシステムも成熟している。

### AI処理: Python

CLIPなどのAI関連ライブラリはPythonエコシステムが圧倒的に充実しているため、画像特徴量抽出部分はPythonで実装する。

### Go-Python連携方式: gRPC

- Python側を独立したCLIPサービスとして実装し、GoからgRPCで呼び出す
- 採用理由: Protobufによる型安全、高速なバイナリ通信、将来的にGPUサーバーへ分離可能
- M1段階では同一マシン上で両プロセスを起動。M2以降で必要に応じてコンテナ化

### フロントエンド: React

モダンなフレームワークで、エコシステムが成熟している。既存の使用経験があるため、UI実装に集中できる。

### データベース: PostgreSQL + pgvector

- メタデータ用RDBとベクトルストアを統合管理できるため、運用負荷が低い
- pgvectorによりCLIP出力ベクトルをPostgreSQL上で扱える
- RDBとしての成熟度が高く、ナレッジが豊富

### 画像特徴量: CLIP

作品に付与されるタグは作者が指定したメインモチーフ（キャラクター名など）に偏り、雰囲気・構図・細部の描写といった情報は欠落しがち。CLIPは画像から雑多な特徴を統合的に捉えられるため、好みの細やかな表現に適する。

### ベクトル検索: hnswlib（または pgvector の HNSW インデックス）

データ量が増加するとベクトル検索の速度と精度のトレードオフが発生する。hnswlibはグラフ構造でノードを接続し、階層的に上層では大きく飛び下層では細かく探索することで、高速かつ高精度な近似近傍探索を実現する。

## データソース

### Wallhaven API

公式に開発者向けAPIを公開しており、規約上クリーンに利用できる。

**実装前確認事項**: API利用規約と画像の hotlinking ポリシーを確認する。Wallhavenの元画像が削除された場合のリンク切れ対策（404検出 + 定期的なURL生存確認）も必要。規約上 hotlinking が不可だった場合は、いいね済み画像のみ低解像度サムネを自前配信する代替案に切り替える。

#### pixivを採用しない理由

pixivは公式APIを一般公開しておらず、スクレイピングは規約違反となる可能性が高い。商業的な権利関係とイラストレーター保護の観点から、サードパーティアプリの自由度は低い。

#### 手持ち画像アップロード

ユーザーがインターネット上のソース不明な画像を保有しているケースに対応するため、アップロード経由でも好み学習を行えるようにする（任意機能）。

### 画像データの取り扱い方針

画像そのものは永続保存しない。ベクトル化のためだけに一時ダウンロードし、処理後は削除する。DBに保存するのはURLとベクトルのみ。

```
Wallhavenから画像を一時ダウンロード
        ↓
CLIPでベクトル化
        ↓
ベクトル + URL + メタデータをDBに保存
        ↓
画像データは削除

※ 画像表示時はWallhavenのURLから直接配信
```

これによりストレージコストをほぼゼロに抑えつつ、ベクトル検索を自前のDBで完結させる。

hotlinking不可の場合の代替: いいね済み画像のみサムネイル（小サイズ）をオブジェクトストレージに保存し、一覧表示時は自前配信・詳細表示時はWallhavenにリダイレクト。

## 学習ループの設計

```
[初期段階]
ランダム or 人気順で画像を提示
        ↓
ユーザーが いいね / スキップ
        ↓
反応データを蓄積（PostgreSQL）

[学習開始閾値を超えた段階]
反応データから好みベクトルを算出
        ↓
CLIP埋め込み済み画像群から類似検索（pgvector / hnswlib）
        ↓
推薦＋推薦理由を提示
        ↓
ユーザーの反応で好みベクトルを更新

[加速オプション]
手持ち画像アップロード → CLIP埋め込み → 初期ベクトルに組み込み
```

### コールドスタート戦略

- 学習開始閾値: **いいね10件 or 総反応30件**のいずれか先に達した時点で好みベクトル算出を開始
- 初期表示は Wallhaven API の `sorting=toplist`（views/favoritesベース）
- フィルターバブル予防のため、初期表示はランダムサンプリングと人気順を半々で混合

### 好みベクトルの算出方針

- **基本方針**: いいね画像のCLIP埋め込みを重み付き平均（新しい反応ほど重みを大きくする時間減衰）
- **ネガティブの扱い**: スキップ画像はRocchio法の負例として軽い負の係数で差し引く
- **複峰性への対応**: 反応データが一定量を超えたらk-meansで2〜3クラスタに分割し「系統別の好み」として扱う（M2後半 or M3）
- **更新頻度**: いいね/スキップごとに即時更新するインクリメンタル方式

### 探索と活用のバランス

類似検索のみだとフィルターバブル化し「同じような画像ばかり」になる。

- 推薦結果の20%を意図的に「好みベクトルから離れた画像」「人気順上位の未提示画像」で埋める（ε-greedy風）
- M3以降で必要に応じてMMR（Maximal Marginal Relevance）等の多様性確保アルゴリズムを検討

## 評価指標

「精度が上がる」を測るための指標。M1から `feedback_events` テーブルに反応を記録し、SQLで集計できるようにしておく。

| 指標 | 定義 | 意味 |
|------|------|------|
| いいね率 | いいね数 / 提示数 | 学習が機能しているかの基本指標 |
| 多様性スコア | 提示画像群のCLIP埋め込みの平均ペアワイズ距離 | フィルターバブル検出 |
| 発掘率 | いいね画像のうちWallhaven人気順下位に属する割合 | 「掘り出しもの」が見つかっているか |

## 開発スコープ

### マイルストーン1（最初に着手）

Wallhaven APIから画像を取得して表示するだけの最小UI。

- 画像取得とキャッシュ
- 一覧表示
- いいね / スキップのUIと反応記録

### マイルストーン2

CLIPによる特徴量抽出と類似検索の組み込み。

### マイルストーン3

推薦理由の可視化UI。

## 未確定の事項

- 推薦理由の可視化方法の最終確定（M3で (a)(b)(c) を評価して採用判断）
- 認証実装時期（M3以降、実装方式は未定）
- ホスティング先

---

作成日: 2026-05-15
作成者: 藤田 雄飛 (Yuhi Fujita)
