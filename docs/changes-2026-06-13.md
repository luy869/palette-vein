# 変更詳細記録 — 2026-06-13

自律改善セッション第2回。Explore サブエージェントによるコードベース全体調査をもとに、
信頼性改善3コミット + ドキュメント2件を実施。
根本的な改善（アーキテクチャ変更を伴うもの）は `proposals-2026-06-13.md` に分離して
ユーザー判断待ちとした。

---

## 1. admin統計の1クエリ化・認証Cookie生成失敗の伝播・パレット集計の修正

**コミット**: `e937e9f`

### 1-1. handleAdminStats: 9回のDB往復を1クエリに統合

**背景**: AdminDashboard は3秒間隔でポーリングしており、1回の表示更新で9個の
`COUNT(*)` / `pg_*_size()` クエリが直列実行されていた（9往復 × 3秒間隔）。

**変更**: スカラーサブクエリで9つの値を単一 `SELECT` にまとめ、1回の `Scan` で取得。
エラー時は `slog.Error` でログを残す。

**効果**: DB往復が 9 → 1。直列実行による累積レイテンシも解消。

### 1-2. setAuthCookie: トークン生成失敗の黙殺を修正

**背景**: `auth.GenerateToken` が失敗した場合、`setAuthCookie` は何もせず return し、
ハンドラは **Cookieなしで 200/201 を返していた**。ユーザーから見ると
「登録/ログイン成功したのに直後の全APIが401」という意味不明な状態になる。

**変更**: `setAuthCookie` が `error` を返すように変更し、register / login の
両呼び出し側で 500 + `slog.Error` を返す。

### 1-3. handleProfilePalette: バブルソート除去 + scanエラーログ

- O(n²) の手書きバブルソートを `sort.Slice` に置換（色数は最大500程度で実害は
  なかったが、標準的でないコードを排除）
- `rows.Scan` 失敗時に黙って `continue` していた箇所に `slog.Warn` を追加
  （部分データ返却の設計は維持）

---

## 2. 面接用ガイド + 根本的改善の提案書

**コミット**: `d428824`

- `docs/interview-guide.md` — 面接でプロジェクトを説明するためのガイド。
  エレベーターピッチ（30秒/2分）、技術選定の理由と比較、深掘りされやすいトピックの
  回答例（推薦アルゴリズム、HNSW、並列処理、SQL最適化、gRPC設計）、
  苦労した点と解決策、数字・規模感、アーキテクチャ図
- `docs/proposals-2026-06-13.md` — アーキテクチャ変更を伴う7つの改善候補を
  選択肢・トレードオフ付きで整理。推奨: プロフィールキャッシュ（オンメモリTTL）と
  k-means複峰性対応を採用、RANDOM()改善とembedder優先度キューは見送り

---

## 3. 全DBエラー箇所への slog ログ追加

**コミット**: `5d51aa3`
**実装**: Sonnet サブエージェントに委任（メインセッションで差分レビュー済み）

**背景**: `http.Error(w, "db error", 500)` が30箇所あり、うち18箇所はログが
一切なかった。本番でエラーが起きても「db error が返った」以上の情報がなく追跡不可能。

**変更**: handlers.go（9箇所）/ search.go（9箇所）に
`slog.Error("<ハンドラ>: <操作> failed", "error", err, "user_id", userID)` を追加。
recommend.go は既にログがあったため変更なし。ロジック変更は一切なし。

---

## 4. 画像検索のCLIPタイムアウト短縮

**コミット**: `2914cea`

**背景**: ユーザー向けの画像アップロード検索（`POST /api/search/image`）が使う
`EmbedBytes` のタイムアウトが、バックグラウンド埋め込みと同じ60秒だった。
フロントエンドは30秒で先にタイムアウトするため、CLIPサービスがハングすると
ユーザーには30秒でエラーが見えるのに、サーバー側の goroutine と gRPC 接続は
さらに30秒間無駄に占有され続ける。

**変更**: `EmbedBytes` を30秒に短縮。バックグラウンド用の `Embed`（画像URL
ダウンロード込み）は60秒のまま、テキスト検索用の `EmbedText` は既存の10秒のまま。

---

## Explore調査で却下した指摘（記録として）

| 指摘 | 却下理由 |
|------|---------|
| feedback_events に (user_id, image_id) インデックス追加 | 既存の `UNIQUE(user_id, image_id, kind)` がプレフィックスとして機能する |
| Wallhaven APIキーの必須化 | 匿名アクセス（45 req/min）は設計上の意図。CLAUDE.mdに明記あり |
| ProfilePalette の hex 値バリデーション（CSSインジェクション） | colors はWallhaven API由来でサーバー側保存。ユーザー入力経路がない |
| JWT_SECRET の長さチェック | 存在チェックは実装済み。長さの強制は運用判断のため提案書にも含めず |

---

## 参考資料

- `docs/proposals-2026-06-13.md` — 今回見送った根本的改善の検討資料
- `docs/changes-2026-06-11.md` — 前回（信頼性・UI/UX改修）の詳細記録
- Explore サブエージェント調査レポート（セッション内、6観点・約25件の指摘）
